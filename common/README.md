# Common Application

This application contains reusable components shared across different business domains.

Currently reserved for:
- Custom exception handlers
- Shared permissions
- Pagination classes
- Constants
- Utility functions
- Base models

No shared components were required for the current implementation except logger.